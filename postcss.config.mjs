/**
 * This file contains the configuration for PostCSS.
 * It includes plugins used by Tailwind CSS.
 */

/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
  },
};

export default config;
